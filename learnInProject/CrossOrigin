@CrossOrigin


	解决跨域问题
	
	两个参数：
		origins：允许可以访问的域列表   origins="http://domain2.com"
		maxAge：准备响应前的缓存持续的最大时间(以秒为单位)   maxAge = 3600
		
	如果正在使用spring security，需要确保在spring安全级别启用CORS，并允许它利用Spring MVC级别定义的配置
		@EnableWebSecurity
		public class WebSecurityConfig extends WebSecurityConfigurerAdapter {
		
		    @Override
		    protected void configure(HttpSecurity http) throws Exception {
		        http.cors().and()...
		    }
		}
		
	
		
